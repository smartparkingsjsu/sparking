<%= form_with(model: event, local: true) do |form| %>
  <% if event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(event.errors.count, "error") %> prohibited this event from being saved:</h2>

      <ul>
      <% event.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :booking_id %>
    <%= form.text_field :booking_id, id: :event_booking_id %>
  </div>

  <div class="field">
    <%= form.label :confidence %>
    <%= form.text_field :confidence, id: :event_confidence %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
